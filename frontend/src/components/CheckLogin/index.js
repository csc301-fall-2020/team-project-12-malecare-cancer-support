/* This file just exports the component right now, so that we can more
   conveniently import components in other locations. Might need to add to  the
   file later. */

export { default } from "./checkLogin.js";
